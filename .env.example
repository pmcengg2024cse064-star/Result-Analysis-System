# Environment Configuration (No secrets needed)
# This file shows the structure of environment variables
# Copy to .env.local to use, but this app doesn't require any env vars for basic functionality

# Optional: Analytics
NEXT_PUBLIC_ANALYTICS_ID=

# Optional: API base URL (if you add a backend in future)
NEXT_PUBLIC_API_URL=http://localhost:3000
